fastapi
uvicorn
pydantic
pyyaml>=6.0
neo4j
openai>=1.40
anthropic>=0.34
sse-starlette>=2.1
numpy>=1.26
python-dotenv>=1.0
# Optional — used by graph_rag.cache when REDIS_URL is set. The import is
# guarded; the service runs fine without Redis (in-process LRU only).
redis>=5.0
# CA bundle for TLS Redis (rediss://) — macOS Python can't resolve the
# system trust store, so we point redis-py at certifi.where().
certifi>=2024.2.2
# Optional — used by src.r2_uploader when R2_* env vars are set
# (Cloudflare R2 is S3-compatible). The import is guarded; missing
# package or missing env both degrade to local-file-only logging.
boto3>=1.34

# Clerk session-token verification (src/auth.py). Every LLM endpoint is
# gated on a verified token so this service can't be used as an open proxy
# to the operator's API keys.
clerk-backend-api>=3.0
